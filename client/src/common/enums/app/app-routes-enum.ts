enum AppRoutes {
    ALL = "/:catchAll(.*)",
    SIGN_IN = "/signIn",
    SIGN_UP = "/signUp",
    PRODUCTS = '/products',
    RECIPES = "/recipes",
    RECIPE = "/recipes/",
}

export {AppRoutes}