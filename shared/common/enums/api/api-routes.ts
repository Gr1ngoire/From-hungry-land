enum ApiRoutes {
    API = "/api",
    AUTH = "/auth",
    PRODUCTS = "/products",
    PRODUCT_TAGS = "/product-tags",
    RECIPES = "/recipes",

    RECIPE = "/recipes/:id",
}

export {ApiRoutes}